Original package repository: [`xwayland-satellite`](https://gitlab.archlinux.org/archlinux/packaging/packages/xwayland-satellite).

The changes are:
 - download PR [`#423`](https://github.com/Supreeeme/xwayland-satellite/pull/423)
 - set `CARGO_HOME` environment variable to a local file to avoid polluting user's `home` directory
